@inject PostService _postService
@page "/blog"

<Container>
    <BlogSearch/>
    
    <div class="grid">
        @foreach (var post in _postService.Posts) {
            <PostCard Post="@post"/>
        }
    </div>
    
    <SocialIcons/>
</Container>

@code {
    // TODO: Fix the layout
}
